# Resume-Ready Bullets

Use these bullets directly in your resume or LinkedIn project section.

- Built a production-style anime recommendation platform in Python with both CLI and REST API interfaces, enabling reusable model logic across interactive and service-based workflows.
- Designed a hybrid content-based ranking pipeline combining TF-IDF description vectors, genre overlap, seasonal matching, and normalized numeric metadata for higher recommendation relevance.
- Implemented robust data preprocessing with schema validation, HTML sanitization, null handling, and deterministic normalization for reliable model inputs.
- Improved search experience using typo-tolerant fuzzy matching for titles and genres, reducing user input friction in conversational and API use cases.
- Added automated quality gates (unit tests + lint checks + GitHub Actions CI) to enforce engineering standards and prevent regressions.
- Refactored the codebase into modular layers (preprocessing, ranking engine, interfaces, config), making the project easier to maintain, test, and extend.

## ATS Keyword Pack

Python, Machine Learning, Recommendation Systems, NLP, Flask API, scikit-learn, pandas, TF-IDF, cosine similarity, CI/CD, unit testing, data preprocessing, software architecture
